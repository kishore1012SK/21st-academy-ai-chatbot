server {
    listen 80;
    server_name 21stacademy.in www.21stacademy.in;

    # --- Static site (your existing career-mapping-workshop site, untouched) ---
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # --- Backend API ---
    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Needed so streamed chat tokens aren't buffered into one chunk
        proxy_buffering off;
        proxy_read_timeout 120s;
    }

    location /health {
        proxy_pass http://backend:8000/health;
    }

    # Basic hardening
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header Referrer-Policy strict-origin-when-cross-origin;

    client_max_body_size 25M;
}

# --- HTTPS (uncomment once you have certs, e.g. via certbot) ---
# server {
#     listen 443 ssl;
#     server_name 21stacademy.in www.21stacademy.in;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ... same location blocks as above ...
# }
